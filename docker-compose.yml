services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./service-account.json:/app/service-account.json
    env_file: .env

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
